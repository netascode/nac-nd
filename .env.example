# nac-nd credentials only.
#
# Connection, domain, fabric(s), TLS and timing live in `nac-nd.yaml` — copy
# `config.example.yaml` to get started. A filled-in `.env` is gitignored.
#
# Precedence: CLI → environment variable → nac-nd.yaml → `.env`

ND_USER=admin
ND_PASSWORD=change-me
