# Examples

| File | Purpose |
| --- | --- |
| `minimal-change.json` | Small APIC MO JSON for trying `nac-nd prechange` without a Terraform plan |

Do not commit real fabric plans or files that contain credentials or Terraform variable values.
